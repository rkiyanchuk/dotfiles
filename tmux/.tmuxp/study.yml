---
session_name: study
start_directory: "${HOME}/workspace/src"
windows:

  - window_name: playground
    start_directory: "${HOME}/workspace/src/playground"
    focus: true
    panes:
      - shell_command:
        - git status

  - window_name: cryptography1
    start_directory: "${HOME}/workspace/src/courses/cryptography1"
    focus: true
    panes:
      - shell_command:
        - git status

  - window_name: algorithms
    start_directory: "${HOME}/workspace/src/courses/algorithms"
    focus: true
    panes:
      - shell_command:
        - git status
